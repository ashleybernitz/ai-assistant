# Extend the official Ollama image
FROM ollama/ollama

# Install curl
RUN apt-get update && apt-get install -y curl

# Optional: clean up to reduce image size
RUN apt-get clean && rm -rf /var/lib/apt/lists/*
